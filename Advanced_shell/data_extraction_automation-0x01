#!/bin/bash

jq -r '
  (.types[0].type.name | ascii_upcase[:1] + .[1:]) as $type |
  "Pikachu is of type \($type), weighs \(.weight/10 | tostring | sub("\\.0$";""))kg, and is \(.height/10 | tostring | sub("\\.0$";""))m tall."
' data.json